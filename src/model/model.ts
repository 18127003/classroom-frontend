export interface Classroom{
    name: string,
    code: string|undefined,
    part: string,
    id: number|undefined,
    room: string,
    topic: string
}

export interface Account {
    name: string,
    id: number
}